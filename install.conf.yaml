- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zprofile:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/dev

- shell:
     -
       command: ./setup/homebrew.zsh
       stdout: true
       stderr": true
     - [git submodule update --init --recursive, Installing submodules]

